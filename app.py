import streamlit as st
from modules import supply_demand, elasticity, production_cost, quiz

# ============================
# إعداد الصفحة ودعم RTL
# ============================
st.set_page_config(page_title="منصة الاقتصاد الجزئي", layout="wide")

st.markdown("""
    <style>
    body {
        direction: RTL;
        text-align: right;
        font-family: 'Arial', sans-serif;
    }
    .stButton>button {
        text-align: center;
    }
    .stSlider>div>div>div>div {
        direction: LTR;  /* slider values from left to right */
    }
    </style>
""", unsafe_allow_html=True)

st.title("📘 منصة مبادئ الاقتصاد الجزئي التفاعلية (محدثة)")

# ============================
# القائمة الجانبية
# ============================
st.sidebar.title("القائمة الرئيسية")
menu = st.sidebar.radio(
    "اختر الفصل",
    [
        "العرض والطلب",
        "المرونة",
        "دالة الإنتاج والتكاليف",
        "اختبار تفاعلي"
    ]
)

# ============================
# الفصل 1: العرض والطلب
# ============================
if menu == "العرض والطلب":
    st.header("📈 الفصل الأول: العرض والطلب")
    st.write("""
    في هذا الفصل يمكنك تعديل السعر والكمية ورؤية تأثير ذلك على منحنيات العرض والطلب.
    """)
    supply_demand.run()  # يفترض أن module يقدّم التفاعلية مع Plotly أو matplotlib

# ============================
# الفصل 2: المرونة
# ============================
elif menu == "المرونة":
    st.header("📊 الفصل الثاني: المرونة")
    st.write("""
    احسب مرونة الطلب السعرية، الدخلية، والتقاطع مع تفسير مباشر للنتيجة.
    """)
    elasticity.run()  # يفترض أن module يتضمن sliders وحسابات تفاعلية

# ============================
# الفصل 3: دالة الإنتاج والتكاليف
# ============================
elif menu == "دالة الإنتاج والتكاليف":
    st.header("🏭 الفصل الثالث: دالة الإنتاج والتكاليف")
    st.write("""
    مثال تفاعلي لحساب الإنتاج الكلي والتكاليف مع رسم منحنيات الإنتاج والتكاليف.
    """)
    production_cost.run()  # module يحتوي على sliders للرأس المال والعمالة ورسم تفاعلي

# ============================
# الفصل 4: الاختبار التفاعلي
# ============================
elif menu == "اختبار تفاعلي":
    st.header("📝 الفصل الرابع: الاختبار التفاعلي")
    st.write("""
    اختبر معلوماتك في الاقتصاد الجزئي. سيتم احتساب النقاط ويمكنك تحميل النتائج بصيغة CSV.
    """)
    quiz.run()  # module يحتوي على الأسئلة والتتبع والتصدير CSV
