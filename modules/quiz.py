import streamlit as st

def run():

    st.header("📝 اختبار قصير")

    score = 0

    q1 = st.radio(
        "عند ارتفاع السعر فإن الكمية المطلوبة:",
        ["تزداد", "تنخفض", "لا تتغير"]
    )

    if st.button("إرسال الإجابات"):

        if q1 == "تنخفض":
            score += 1

        st.subheader(f"النتيجة: {score}/1")
